// math_utils_module.ms - A simple math utilities module

function add(a, b) {
    return a + b;
}

function multiply(a, b) {
    return a * b;
}

function square(x) {
    return x * x;
}

// Set some module variables
module_version = "1.0";
module_name = "Math Utilities";

print("Math utilities module loaded!");